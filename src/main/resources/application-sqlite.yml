spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:${user.dir}\data.db
    username:
    password:
  jpa:
    database-platform: com.uniloftsky.springframework.taskmanager.config.sqlite.SQLiteDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false